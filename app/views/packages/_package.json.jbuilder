json.extract! package, :id, :title, :description, :price, :subscription, :subscription_interval_count, :subscription_interval, :stripe_subscription_id, :created_at, :updated_at
json.url package_url(package, format: :json)
